# Use a single DATABASE_URL connection string (e.g., provided by NeonDB/Heroku):
DATABASE_URL=postgres://user:password@host:5432/dbname

# Keep TLS verification enabled by default. Set to 'false' only if necessary.
DB_REJECT_UNAUTHORIZED=false

PORT=3000
